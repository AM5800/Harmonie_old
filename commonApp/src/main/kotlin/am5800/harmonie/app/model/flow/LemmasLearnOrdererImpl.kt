package am5800.harmonie.app.model.flow

import am5800.common.Lemma

class LemmasLearnOrdererImpl : LemmasLearnOrderer {
  override fun reorder(lemmas: List<Lemma>): List<Lemma> {
    return lemmas
  }
}